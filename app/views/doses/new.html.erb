<h1>Doses#new</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %> <%= f.submit %>
  <%= link_to 'Back', cocktails_path %>

<% end %>
